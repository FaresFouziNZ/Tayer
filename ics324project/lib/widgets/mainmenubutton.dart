import 'package:flutter/material.dart';

Container MainMenuButton() {
  return Container(
    child: Text('asd'),
    padding: const EdgeInsets.all(15),
    width: 300,
    height: 100,
    //color: Colors.amber,
    decoration: BoxDecoration(
        border: Border.all(width: 3, color: Colors.blue), borderRadius: const BorderRadius.all(Radius.circular(5.0))),
  );
}
